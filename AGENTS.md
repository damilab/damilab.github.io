# DAMI Lab 홈페이지 작업 규칙

(이 파일은 CLAUDE.md 와 같은 내용이다. 어느 도구로 작업하든 같은 규칙을 따른다.)

이 저장소를 고치기 전에 [WEBSITE_MAINTENANCE.md](WEBSITE_MAINTENANCE.md) 를 먼저 읽는다.
페이지별 수정 위치, 작업 순서, 검수 방법이 거기에 있다.

아래는 그중에서도 어기면 되돌리기 어려운 것들이다.

## 논문 PDF는 저장소에 넣지 않는다

저장소가 이미 524MB이고 GitHub 권장 한도는 1GB다. **git은 파일을 지워도
히스토리에 남겨 용량이 회수되지 않는다.** 한 번 커밋한 큰 파일은 되돌릴 수 없다.

지금 용량의 절반이 논문 PDF와 발표 PPT다. 가장 큰 것 하나가 24MB로,
갤러리 사진 100장과 맞먹는다.

| 무엇 | 어디에 |
| --- | --- |
| 논문 원문 | **링크만 건다.** arXiv, OpenReview, ACL Anthology, DBpia, KCI |
| 우리가 만든 포스터·슬라이드 | PDF로 변환해 저장소에 (PPTX 20MB → PDF 2~3MB) |
| 아주 큰 파일, 영상 | 연구실 서버에 두고 링크 |
| 갤러리 사진 | 폭 1400px로 줄여 `images/gallery/` 에 (장당 약 250KB) |

용량 때문만이 아니다. 학회·저널 최종본을 홈페이지에 직접 올리는 것은 출판사 정책에
어긋나는 경우가 많다. 저자가 올린 arXiv 판이나 학회 공식 페이지 링크는 언제나 괜찮다.

**사용자가 논문 PDF를 주면서 올려 달라고 하면, 커밋하기 전에 링크로 걸 수 있는지
먼저 묻는다.** arXiv나 학회 페이지 주소를 받아 그것을 건다.

## HTML을 스크립트로 일괄 편집할 때는 바이트 단위로

줄바꿈이 파일마다 CRLF, LF, CR 단독까지 섞여 있다. 텍스트 모드로 읽고 쓰면
diff가 수천 줄로 부풀고 일부 파일은 줄이 사라진다.

`open(path,'rb')` 로 읽고, 앵커는 정규식으로 찾은 뒤 그 블록이 쓰는 줄바꿈을 그대로
따라가 `open(path,'wb')` 로 쓴다. 작업 후 `git diff -U0 | grep -c '^-[^-]'` 로
삭제된 줄이 0인지 확인한다.

## 여러 세션이 같은 작업 디렉토리를 쓴다

`git add -A` 와 `git commit -a` 를 쓰지 않는다. 경로를 명시해 add 하고,
커밋 전 `git status --short`, 커밋 후 `git show --stat HEAD` 로 확인한다.

## 이 파일을 고칠 때

`CLAUDE.md` 와 `AGENTS.md` 는 이후 모든 세션의 행동을 정하는 파일이다.
여기에 규칙을 넣거나 빼거나 고치려면 **사용자에게 먼저 무엇을 어떻게 바꿀지 알리고
승인을 받는다.** 작업하다 필요해 보인다는 이유로 임의로 고치지 않는다.
두 파일은 같은 내용을 유지하므로 한쪽만 고치지 않는다.

## 확인되지 않은 것은 비워 둔다

행사 이름, 장소, 날짜를 모르면 그럴듯하게 지어내지 말고 비워 둔 채로 사용자에게 묻는다.
